<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            {{ encore_entry_link_tags('boot') }}
            {{ encore_entry_link_tags('custom') }}
            {{ encore_entry_link_tags('menu') }}
        {% endblock %}
    </head>
    <body>
        {% include('Espace_Client/Menu.html.twig') %}
        {% block body %}{% endblock %}
        {% block javascripts %}
            {{ encore_entry_script_tags('lemenu') }}
        {% endblock %}
    </body>
</html>
